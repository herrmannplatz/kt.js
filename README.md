# kt.js

```
const list = kt.listOf([1, 2, 3])

list.contains(1) // true
list.containsAll([1, 2]) //
list.isEmpty() // false
list.asReversed() // [3, 2, 1]
list.count() // 3
list.filterNotNull() // [1, 2, 3]
list.first(el => el > 1) // 2
```
